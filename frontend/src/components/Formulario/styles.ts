import styled, { css } from 'styled-components';

export const MainContainer = styled.div`
  ${() => css`
    width: 100%;
    height: 100%;

    .form-group {
      margin-bottom: 2rem;
    }
  `}
`
